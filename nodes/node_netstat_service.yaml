 apiVersion: v1
 kind: Service
 metadata:
  name: eth-netstat-service
  namespace: ethereum-ns
  labels:
    app: eth-geth-miner
 spec:
  type: NodePort
  selector:
    app: eth-geth-miner
  ports:
   - protocol: TCP
     port: 30303
     nodePort: 30303