apiVersion: v1
kind: Service
metadata:
  name: eth-apache-service
  namespace: ethereum-ns
  labels:
    app: eth-apache
spec:
  selector:
    app: eth-apache
  type: NodePort
  ports:  
  - name: http
    port: 80
    targetPort: 80
    nodePort: 30036
    protocol: TCP